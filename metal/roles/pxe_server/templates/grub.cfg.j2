set timeout=1

menuentry '{{ iso_url | basename | splitext | first }} (PXE)' {
    linux vmlinuz \
        ip=dhcp \
        inst.ks=http://{{ ansible_default_ipv4.address }}:1234/init-config/${net_default_mac}.ks
    initrd initrd.img
}
